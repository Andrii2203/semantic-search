# Agents

The entry point for every AI agent and contributor is `CLAUDE.md` in the repository root.

This file exists only so that tools which look for `AGENTS.md` find the same starting place. It holds
no rules of its own and is never updated with content. Read `CLAUDE.md`.
