node_modules/
data/
.env
*.db
coverage/
.DS_Store

# Generated test fixtures (synthetic resumes + ground truth for eval)
test-data/
# BEIR datasets are fetched, not vendored. See docs/plans/public-benchmark.md section 5.
eval/beir/

# Model weights, downloaded not vendored
.cache/

# Throwaway probes, never part of the record
scratch/
